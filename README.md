# OCULIS
See what's behind the link before you visit it.  A web platform that takes a suspicious URL, inspects it in an isolated sandbox (DNS/TLS/HTTP, redirect chain, headless-browser render, DOM/network capture), scores the risk, and shows you evidence-backed findings — so you never have to open the link yourself.
